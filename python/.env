# Environment (development/production)
ENVIRONMENT=development

# Allowed origins (comma-separated list of URLs)
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# WebSocket settings (in seconds)
WS_PING_INTERVAL=20
WS_PING_TIMEOUT=20
WS_CLOSE_TIMEOUT=20

# Game settings
GAME_TIMEOUT=60 